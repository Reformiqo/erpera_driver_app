# Migration patches intentionally unregistered: they target the legacy
# "Cowberry *" doctype names, which on a bench that also has the separate
# `cowberry_app` would hijack ITS doctypes. Fresh installs create the
# correctly-named doctypes directly from JSON. For the original v15 site,
# run the patches under patches/v0_0_*/ manually as a one-off.
[pre_model_sync]

[post_model_sync]
