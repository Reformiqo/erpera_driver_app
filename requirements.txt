frappe
# Pin <81 — setuptools 81 removed pkg_resources which Frappe's bundled
# dropbox dep still imports. Without the cap, pip pulls 81+ and migrate
# crashes at "Updating DocTypes for frappe".
setuptools<81
